import React, {useState, useEffect} from 'react';

const RestDetails = () => {
	// const inputs = props.location.state;
	const [info, setInfo] = useState([]);
	console.log('here');
	// useEffect(() => {
	// 	fetch(`api/getdetails?id=${inputs.rest.id}`).then(res => res.json())
	// 	.then(data => {
	// 		setInfo(data);
	// 	});
	// });

	return (
		<div>
			<h4>Hi</h4>
		</div>
	);
}

export default RestDetails;